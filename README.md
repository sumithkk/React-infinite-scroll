# React Infinite scroll + image lazy loading

Implementing infinite scroll and image lazy loading with the `HTML` `IntersectionObserver` API.

cd server: npm install
           node server.js

cd client: npm install
           npm start
